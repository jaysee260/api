{
  "name": "api",
  "version": "1.0.0",
  "description": "an express api in node.js",
  "main": "server.js",
  "scripts": {
    "start": "cross-env NODE_ENV=prod HTTP_PORT=3000 HTTPS_PORT=3030 node server.js",
    "dev": "cross-env NODE_ENV=dev HTTP_PORT=8000 HTTPS_PORT=8080 nodemon server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cross-env": "^5.2.0",
    "express": "^4.16.4"
  },
  "devDependencies": {
    "nodemon": "^1.18.11"
  }
}
